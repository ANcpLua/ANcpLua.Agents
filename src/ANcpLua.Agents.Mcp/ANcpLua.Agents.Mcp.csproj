<Project Sdk="ANcpLua.NET.Sdk">
    <PropertyGroup>
        <RootNamespace>ANcpLua.Agents.Mcp</RootNamespace>
        <PackageId>ANcpLua.Agents.Mcp</PackageId>
        <Description>Qyl-prefixed Model Context Protocol client facades for Microsoft Agent Framework consumers.</Description>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <PackageTags>maf;agents;microsoft-agents;mcp;model-context-protocol;client</PackageTags>
        <!--
            CA2000 false positive in QylHttpMcpClient: HttpClientTransport is
            IAsyncDisposable and owns an HttpClient from construction. On success
            McpClient.CreateAsync takes ownership (the client disposes the transport on
            DisposeAsync); on failure ConnectAsync disposes it explicitly in a catch.
            CA2000 cannot model the success-path ownership transfer into CreateAsync, so it
            still flags the `new HttpClientTransport(...)` site. StdioClientTransport is not
            disposable, so the stdio bundle is unaffected.
        -->
        <NoWarn>$(NoWarn);CA2000</NoWarn>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="ANcpLua.Roslyn.Utilities"/>
        <PackageReference Include="Microsoft.Agents.AI"/>
        <PackageReference Include="Microsoft.Extensions.AI"/>
        <PackageReference Include="ModelContextProtocol"/>
    </ItemGroup>

</Project>
